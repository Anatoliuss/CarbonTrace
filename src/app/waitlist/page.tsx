import WaitlistFullForm from '@/components/WaitlistFullForm'

export default function WaitlistPage() {
  return (
    <main className="min-h-screen flex flex-col items-center justify-center py-12 px-4">
      <WaitlistFullForm />
    </main>
  )
}